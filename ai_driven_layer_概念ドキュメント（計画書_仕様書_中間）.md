# Content Projection Layer 概念ドキュメント

## 1. 位置づけと目的

### 1.1 本ドキュメントの性質
本ドキュメントは、**事業計画書と技術仕様書の中間**に位置する。
- 企画意図・思想・背景を明確にする
- 実装可能性を前提とした構造・責務を定義する
- ただし詳細なAPI仕様やクラス設計までは踏み込まない

### 1.2 背景
既存CMS（Content Management System）は以下の前提に強く依存している。
- 人間がUIを操作して編集する
- AIは補助的（オプション）な存在
- 保存＝即反映 or 即公開が基本

一方、本プロダクトでは以下が前提となる。
- **AIが編集の主役**である
- ただし**人間による直接編集は必須**
- 編集・確認・反映（ビルド）は明確に分離される

この前提を成立させるために、新たな概念として **Content Projection Layer** を定義する。

---

## 2. Content Projection Layer とは何か

### 2.1 定義と名称の由来
**Content Projection Layer** とは、
> コンテンツ構造・意図・状態を理解した上で、
> 人間およびAIの編集行為を統合的に駆動・制御する中間層

である。

これは「エディター」でも「CMS」でもなく、
**編集・生成・プレビュー・調整・判断を行う"コンテンツ投射レイヤー"**である。

**名称の由来:**
- **Projection（投射/投影）**: 編集意図をプレビュー空間に「投射」し、可視化するという意味


### 2.2 既存概念との違い

| 概念 | 主体 | 特徴 |
|---|---|---|
| CMS | 人間 | UI中心・保存即反映 |
| Headless CMS | 人間 | API分離・構造管理 |
| IDE | 人間＋AI | コード生成・編集 |
| **Content Projection Layer** | **AI主導＋人間介入** | 構造理解・意図駆動・プレビュー統合・反映制御 |

---

## 3. レイヤー構造全体像

### 3.1 レイヤード思想
本プロダクトは以下のレイヤー構造を前提とする。

1. **Presentation Layer**
   - 公開サイト（Next.js 等）
   - SSG / ISR / SSR のいずれか

2. **Content Projection Layer（本概念）**
   - 公開状態とは完全に分離された編集空間
   - 編集意図の解釈と構造理解
   - 人間・AI双方の編集結果を可視化するプレビュー環境
   - 変更差分の管理とビルド可否の判断補助
   - **データフロー**: プレビュー環境で編集されたJSONデータは、ビルドプロセスによってPresentation Layerに反映される

3. **Content Source Layer**
   - Markdown / JSON / DB / CMSデータ
   - Single Source of Truthとしての役割

---

## 4. 編集体験（UX）の前提条件

### 4.1 AIはメイン、しかし排他的ではない
- AI操作は必須機能
- しかし **AIを通さない直接編集も常に可能**
- AIしか操作できない設計は採用しない

理由：
- 現実のクライアント運用では受け入れられない
- 緊急時・微調整時の即応性が落ちる

### 4.2 Content Projection Layer の役割
Content Projection Layer は以下を満たす。
- **ビルドを伴わない即時プレビュー**: 編集内容はプレビュー環境内のJSONデータとして保持され、即座に可視化される
- **公開サイトに影響しない編集環境**: ビルド操作を行うまで、Presentation Layerには何も反映されない
- **人間編集・AI編集の両方を統合的に反映**: どちらの編集方法も同一のプレビュー空間で扱う

---

## 5. ビルドと反映の思想

### 5.1 「ビルド」の定義
本ドキュメントにおける「ビルド」とは、以下の2つの意味を含む。
1. **レイヤー上での確定操作**: ユーザーが編集内容を確定し、公開を意図するUI操作
2. **Next.jsビルドプロセスの実行**: 上記操作をトリガーとして、Next.jsのビルド（ISR再生成など）を行うこと

### 5.2 ビルド乱発は「UXの問題」ではなく「人間の性質」
- 編集者は短いスパンで保存・確認を繰り返す
- 忘れると面倒になるためビルドを走らせがち

これは設計の失敗ではなく、**人間の行動特性**である。

### 5.3 採用方針
- 編集確定 ≠ ビルド
- **ビルドは明示的操作**とする
- ビルド時には以下を必ず可視化
  - 所要時間
  - 公開影響
  - 差分内容

Content Projection Layer は
- 「今ビルドすべきか？」
- 「どの反映方式が適切か？」

を判断・助言する立場に留める。

---

## 6. SSG / ISR / SSR との関係

### 6.1 前提整理
- アクセスとは **不特定多数の閲覧アクセス**
- 認証の有無とは無関係

### 6.2 方式別整理

#### SSG（Static Site Generation：静的サイト生成）
- ビルド時に全ページ生成
- 高速・安全
- 更新頻度が高いと運用負荷が高い

#### ISR（Incremental Static Regeneration：段階的再生成）
- 初回は静的
- アクセス時 or 一定時間後に再生成
- 更新頻度と安定性のバランスが良い

#### SSR（Server Side Rendering：サーバー側描画）
- 常に最新
- サーバー負荷・コスト増

### 6.3 本構想との相性
- 公開：**ISR を基本**
- 編集・確認：Content Projection Layer
- Content Projection Layer が反映方式を意識しない設計

---

## 7. Content Projection Layer の責務

### 7.1 主な役割
- コンテンツ構造の理解と編集意図の解釈
- 差分生成・管理
- ビルド判断の補助
- プレビュー環境の統合管理

### 7.2 やらないこと
- 公開判断の自動実行
- 完全自律的な運用
- 人間の編集権限の剥奪

---

## 8. CMS との関係性

### 8.1 本プロダクトは CMS か？
結論：**CMSではない**。
CMSに対立・競合する概念ではなく、別のアプローチとして提唱する。

### 8.2 CMSとの関係
- **既存CMSの後付け対応**: WordPress等で構築されたサイトに、編集レイヤーを後付けで組み込むことも想定
- **別の価値軸**: 完全なCMS機能ではなく、限定的なUXと開発コストのバランスを重視した概念


### 8.3 既存アプローチとの違い
| 側面 | 既存Headless CMS | 完全自作CMS | Content Projection Layer |
|---|---|---|---|
| AI編集機能 | 未対応/遅れている | 自由に設計可能 | 標準対応 |
| 開発コスト | 低い | 非常に高い | 中程度 |
| 修正の柔軟性 | サービス次第 | 完全自由 | レイヤー単位で自由 |
| 想定ユーザー | 開発者 | 開発者 | 知識なしのユーザも対象 |
| 価値提案 | 安定的運用 | 完全カスタマイズ | AI主導の編集体験 |
| 複雑性 | 低い | 非常に高い | 本番ページよりは高い |

**基本戦略:**
- ウェブサイト制作段階で編集可能箇所を制限
- その箇所のみをContent Projection Layerで編集可能にする
- ビジュアルエディターに近い見た目で、知識のないユーザでも編集しやすい環境を提供
- AIによる編集を比較的簡単に実行できる

---

## 10. まとめ

Content Projection Layer は
- AIを前に出しすぎず
- しかし確実に主役に据え
- 人間の現実的な運用を否定しない

という思想の上に成立する。

これは単なる新CMSではなく、
**編集という行為そのものの再定義**である。

